# README #

`pyvins` is a Python package that provides a set of tools for analyzing the performance of visual-inertial navigation systems. To install, create a virtual environment, enter the `pyvins` directory and run the command 
```
pip install -e .
```
which will install the package in editable mode, along with all necessary dependencies.